<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chengbo.emccomida">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.PrefilActivity"
            android:parentActivityName=".activity.HomeActivity"></activity>
        <activity
            android:name=".activity.EncomendaActivity"
            android:parentActivityName=".activity.HomeActivity" />
        <activity
            android:name=".activity.EmcPendenteActivity"
            android:parentActivityName=".activity.HomeActivity" />
        <activity
            android:name=".activity.InserirPratoActivity"
            android:parentActivityName=".activity.PratoActivity" />
        <activity
            android:name=".activity.PratoActivity"
            android:parentActivityName=".activity.HomeActivity" />
        <activity android:name=".activity.HomeActivity" />
        <activity
            android:name=".activity.RecupPassActivity"
            android:theme="@style/AppThemeNoActionbar" />
        <activity
            android:name=".activity.RegisterActivity"
            android:theme="@style/AppThemeNoActionbar" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/AppThemeNoActionbar" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/AppThemeNoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>